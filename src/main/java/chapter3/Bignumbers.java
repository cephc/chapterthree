package chapter3;

public class Bignumbers {

    //If the precision of the basic integer and floating-point types is not sufficient, you can turn to a couple
    // of handy classes in the java.math package: BigInteger and BigDecimal.
    // classes for manipulating numbers with an arbitrarily long sequence of digits.
    // The BigInteger class implements arbitrary-precision integer arithmetic, and
    // BigDecimal does the same for floating-point numbers
    // Use the static valueOf method to turn an ordinary number into a big number:
    //BigInteger a = BigInteger.valueOf(100);
    // you must use methods such as add and multiply in the big number classes.
    //BigInteger c = a.add(b); // c = a + b BigInteger d = c.multiply(b.add(BigInteger.valueOf(2))); // d = c * (b + 2)

}
